{
  "name": "coldroom-server",
  "version": "1.0.0",
  "private": true,
  "description": "Ingest MQTT telemetry into MySQL and serve a history API + CSV export for the coldroom dashboard.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mqtt": "^5.10.1",
    "mysql2": "^3.11.0"
  }
}
